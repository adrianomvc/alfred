# Toolbar Fixtures

Fixtures generated by `scripts/powershell/workflow/render-toolbar.ps1`.

## Cases
- `fast.txt`
- `safe.txt`
- `execution-first.txt`
- `standard-parallel-units.txt`

Regenerate with:

```powershell
powershell -ExecutionPolicy Bypass -File scripts/powershell/workflow/render-toolbar.ps1 -StatePath <path-to-001-state.md> -Model GPT-5 -Cost "n/a"
```

Validate drift with:

```powershell
powershell -ExecutionPolicy Bypass -File scripts/powershell/validators/validate-toolbar-fixtures.ps1
```
