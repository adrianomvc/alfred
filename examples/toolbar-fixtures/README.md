# Toolbar Fixtures

Fixtures generated by `scripts/powershell/render-toolbar.ps1`.

## Cases
- `fast.txt`
- `safe.txt`
- `execution-first.txt`
- `standard-parallel-units.txt`

Regenerate with:

```powershell
powershell -ExecutionPolicy Bypass -File scripts/powershell/render-toolbar.ps1 -StatePath <path-to-001-state.md> -Model GPT-5 -Cost "n/a"
```

Validate drift with:

```powershell
powershell -ExecutionPolicy Bypass -File scripts/powershell/validate-toolbar-fixtures.ps1
```
