# Toolbar Fixtures

Fixtures generated by `scripts/python/workflow/render-toolbar.py`.

## Cases
- `fast.txt`
- `safe.txt`
- `execution-first.txt`
- `standard-parallel-units.txt`

Regenerate with:

```bash
python scripts/python/workflow/render-toolbar.py -StatePath <path-to-001-state.md> -Model GPT-5 -Cost "n/a"
```

Validate drift with:

```bash
python scripts/python/validators/validate-toolbar-fixtures.py
```
