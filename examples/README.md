# examples/ — sample sigla / iniciativa / demanda

Illustrative only (not real data). Shows the end-to-end flow across HUB and App for a pilot demand.
- `hub-PGTO/` — example HUB for sigla PGTO (artifacts in pt-BR, D47).
- `app-pgto-api/` — example app repo with `.alfred/`.
Pilot: demand `PGTO-142` "novo split de pagamento" (Produto, Standard).
