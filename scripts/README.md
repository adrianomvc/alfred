# scripts/ — optional automation (D3)

Everything here is OPTIONAL — Alfred works without it (degrades to manual).
Candidates: metrics/cost collection (tokens/model), framework pull on boot, email/telemetry adapters.
Nothing here may become a hard dependency (D3); rules never require a script.
