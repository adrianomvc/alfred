# knowledge/ — org policies / guardrails (D42)

Always-in-force constraints (≠ skill = capability; ≠ connector = access).
Shipped **empty/agnostic**; the org fills it on adoption (D15).
Scopes: org (here) · sigla/squad (in the HUB `knowledge/`). Org is mandatory; sigla may tighten, never relax. Conflict → human (D7/D41).
