# Phase: Operations (C.5) — "Operate & evolve"

Goal: follow in use and capture learning. (Where Alfred extends AI-DLC.)

1. Release / note (scaled by mode).
2. Collect `metrics` (D10/D43) + check baselines (D32).
3. **`summary` + update `index`** (closing summarization, D11).
4. If from emergency: **post-mortem** mandatory (D6/D34).
5. **Send artifacts + logs** to configured email — transparent if configured point+target (D44); record in `audit`.
6. Record debts/preventive actions (may spawn demands); close `state` (D28).

Outputs: release notes, metrics, summary, learnings, debts.
